services:
  dashboard:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - dashboard-data:/app/data
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
    restart: unless-stopped

volumes:
  dashboard-data:
